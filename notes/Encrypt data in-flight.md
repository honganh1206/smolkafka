# Encrypt data in-flight

Why? To prevent man-in-the-middle (MITM) attacks e.g., tricking victims like they are talking directly with each other

We use [[TLS]], the successor of SSL, to encrypt data in-flight.
